services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: squad_builder
      POSTGRES_USER: squad
      POSTGRES_PASSWORD: squad
    ports:
      - "5432:5432"
    volumes:
      - squad_builder_pg:/var/lib/postgresql/data

volumes:
  squad_builder_pg:
