[project]
name = "squad-builder-backend"
version = "0.1.0"
description = "Backend for squad-builder"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32.5",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "ruff>=0.15.0",
]
